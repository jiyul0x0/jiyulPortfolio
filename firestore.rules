rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // 방문자는 전부 읽기만 가능, 쓰기는 로그인한 관리자만 가능합니다.
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
